# Capacitor asset sources (local only)

Copy `public/music-data-base-logo.png` to `icon.png` and `splash.png`, then run:

`npx @capacitor/assets generate --android`

iOS icons are under `ios/App/App/Assets.xcassets/`.
